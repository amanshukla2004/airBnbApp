# ===== RENDER PRODUCTION PROFILE =====
# This file is ONLY used when SPRING_PROFILES_ACTIVE=prod is set.

# Database (Render PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d8ciimgg4nts738npieg-a.singapore-postgres.render.com/airbnb_db_omr9?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:airbnb_db_omr9_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:TYpJuAwzRTjVg9JpOtRLKPGKFx5x1NFJ}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# HikariCP
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

# Security
jwt.secretKey=${JWT_SECRETKEY}

# Frontend & Stripe
frontend.url=${FRONTEND_URL}
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}
support.email=support@noxplatform.com
